spring:
  datasource:
    url: jdbc:h2:mem:aluno_check
    username: admin
    password: admin
    driver-class-name: org.h2.Driver
  jpa:
#    hibernate:
#      ddl-auto: update
#    properties:
#      hibernate:
#       dialect: org.hibernate.dialect.H2Dialect
#      show-sql: false
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
      path: /h2
